# Deploy SIMS
1. Run `git status` to confirm pending changes.
2. Stage and commit with a descriptive message.
3. Run `git push`.
4. Confirm the push succeeded and remind user GitHub Pages may take a minute to update.
